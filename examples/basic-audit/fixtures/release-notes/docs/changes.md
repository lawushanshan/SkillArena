# Changes for 1.4.0

- Added bulk archive support for completed widgets.
- Improved validation messages for invalid widget names.
- Deprecated the legacy `POST /widgets/archive` endpoint; use `POST /widgets/bulk-archive` instead.
